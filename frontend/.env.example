# Produkcja (Vercel): publiczny URL backendu na Renderze — BEZ końcowego slasha.
# Ustaw to samo w Vercel → Settings → Environment Variables → VITE_API_URL
# (zmienne VITE_* są wstrzykiwane przy npm run build)
VITE_API_URL=https://twoj-serwis.onrender.com
